# ------------------------------------------------------------------------------
# FILE:        .github/workflows/lint-markdown.yml
# VERSION:     3.6.7
# DESCRIPTION: GitHub Action: Markdown Linting with SARIF Annotations
# AUTHOR:      Stony64 - Bash/Shell Dotfiles Framework
# CHANGES:     3.6.7 - Update action to v20, set fix: false for CI
# ------------------------------------------------------------------------------

name: "📝 Lint Markdown"

on:
  push:
    branches: [main, master, "release/**"]
    paths:
      - "**/*.md"
      - ".markdownlint*"
      - ".github/workflows/lint-markdown.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "**/*.md"
      - ".markdownlint*"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  checks: write # SARIF Upload

jobs:
  markdownlint:
    name: "🔍 Markdown Linting"
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🔍 Run markdownlint-cli2
        uses: DavidAnson/markdownlint-cli2-action@v20
        with:
          config: .markdownlint-cli2.jsonc
          globs: |
            **/*.md
            !node_modules
            !.git
            !**/_exports/**
            !**/test_sandbox/**
            !home/**
          fix: false
        continue-on-error: false

      - name: 📊 Upload SARIF (Annotations)
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: markdownlint-cli2.sarif
          checkout_path: ${{ github.workspace }}

      - name: 📝 Job Summary
        if: always()
        run: |
          echo "### Markdown Linting Status: ${{ job.status }}"

          if [[ "${{ job.status }}" == "success" ]]; then
            echo ""
            echo "✅ **All Markdown files** comply with project style"
            echo "**Files checked:** $(find . -name '*.md' -not -path '*/node_modules/*' -not -path '*/.git/*' -not -path '*/home/*' | wc -l)"
            echo "**Config:** [.markdownlint-cli2.jsonc](.markdownlint-cli2.jsonc)"
            echo "**Rules:** MD001, MD003=atx_closed, MD013=off, MD024=siblings_only"
          else
            echo ""
            echo "❌ **Linting errors found** → Check **Annotations** tab!"
            echo "**Fix locally:** \`markdownlint-cli2 --fix \"**/*.md\"\`"
            echo "**Config:** [.markdownlint-cli2.jsonc](.markdownlint-cli2.jsonc)"
          fi

          echo ""
          echo "---"
          echo "*Triggered:* ${{ github.event_name }} #${{ github.run_number }}"
          echo "*Branch:* ${{ github.ref_name }}"
          echo "*Commit:* ${{ github.sha }}"
        shell: bash
